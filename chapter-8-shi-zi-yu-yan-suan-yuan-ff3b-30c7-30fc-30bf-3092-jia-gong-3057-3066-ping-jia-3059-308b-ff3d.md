> 所謂的programming，就是把變數用各種式子做各種處理以達到目的。為此就需要像數學一樣，用各式各樣的運算元來處理。
> 雖然基本上是跟算數學時一樣，但像是等號(=)的作用就不一樣。又因為C語言的source code的檔案是文字檔，不像數學可以用那麼多種多樣的運算元。所以，ASCII code可處理範圍中的記號有各式各樣的意義，即使是同樣記號也因為場合不同而有不同意思。
> 這一章是說明各式各樣的運算元，在讀完的時候，第七章的範例程式中出現的(character % 16) == 0的意義也可以理解吧。